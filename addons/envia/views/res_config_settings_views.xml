<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_envia" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.envia</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app string="Envia.com" name="envia">
                    <field name="envia_has_api_token" invisible="1"/>
                    <field name="envia_oauth_connected" invisible="1"/>

                    <block title="Connection"
                           help="Link your Odoo store with Envia.com and manage integration credentials.">
                        <setting string="Envia.com"
                                 help="Connect your store with Envia.com. Only administrators can refresh the integration token.">
                            <div class="o_envia_settings_connection_card">
                                <div class="d-flex flex-wrap align-items-center justify-content-between gap-2">
                                    <div class="d-flex flex-wrap align-items-center gap-2">
                                        <div invisible="not envia_oauth_connected"
                                             class="badge rounded-pill text-bg-success o_envia_connection_badge">
                                            <field name="envia_oauth_status_connected"
                                                   nolabel="1"
                                                   readonly="1"/>
                                        </div>
                                        <div invisible="envia_oauth_connected"
                                             class="badge rounded-pill text-bg-secondary o_envia_connection_badge">
                                            <field name="envia_oauth_status_disconnected"
                                                   nolabel="1"
                                                   readonly="1"/>
                                        </div>
                                    </div>
                                    <button name="action_envia_refresh_integration_token" type="object"
                                            string="Refresh token"
                                            class="btn btn-secondary btn-sm"
                                            icon="fa-refresh"
                                            groups="base.group_system"/>
                                </div>
                                <div class="o_envia_settings_version_row d-flex flex-wrap gap-2 mt-2">
                                    <span class="o_envia_settings_version_chip">
                                        <span class="o_envia_settings_version_label">Shop ID</span>
                                        <field name="envia_shop_id_display"
                                               nolabel="1"
                                               readonly="1"
                                               class="oe_inline"/>
                                    </span>
                                    <span class="o_envia_settings_version_chip">
                                        <span class="o_envia_settings_version_label">Company ID</span>
                                        <field name="envia_company_id_display"
                                               nolabel="1"
                                               readonly="1"
                                               class="oe_inline"/>
                                    </span>
                                    <span class="o_envia_settings_version_chip">
                                        <span class="o_envia_settings_version_label">User ID</span>
                                        <field name="envia_user_id_display"
                                               nolabel="1"
                                               readonly="1"
                                               class="oe_inline"/>
                                    </span>
                                    <span class="o_envia_settings_version_chip">
                                        <span class="o_envia_settings_version_label">Odoo</span>
                                        <field name="envia_odoo_version_display"
                                               nolabel="1"
                                               readonly="1"
                                               class="oe_inline"/>
                                    </span>
                                    <span class="o_envia_settings_version_chip">
                                        <span class="o_envia_settings_version_label">Module</span>
                                        <field name="envia_module_version_display"
                                               nolabel="1"
                                               readonly="1"
                                               class="oe_inline"/>
                                    </span>
                                </div>
                            </div>
                        </setting>
                        <setting string="Odoo API key"
                                 help="API key Envia.com uses to call back into Odoo. Generate it here and copy it into your Envia.com integration setup."
                                 groups="base.group_system">
                            <field name="envia_integration_api_key"
                                   widget="envia_api_key"
                                   nolabel="1"
                                   readonly="1"/>
                            <div class="d-flex flex-wrap align-items-center gap-2 mt-2">
                                <button name="action_generate_envia_integration_api_key" type="object"
                                        string="Generate API key"
                                        class="btn btn-secondary btn-sm"
                                        invisible="envia_integration_api_key"
                                        icon="fa-key"/>
                                <span class="text-muted small" invisible="envia_integration_api_key">
                                    Generate a key to configure the Envia.com callback.
                                </span>
                            </div>
                        </setting>
                        <setting string="Envia shipping token"
                                 help="Bearer token for api.envia.com (quoting, labels, tracking). Saved automatically by Envia.com during integration."
                                 groups="base.group_system">
                            <field name="envia_api_token_display"
                                   widget="envia_api_key"
                                   nolabel="1"
                                   readonly="1"/>
                            <div class="text-muted small mt-2"
                                 invisible="envia_has_api_token">
                                No token stored yet. Complete the Envia.com connection or wait for the integration callback.
                            </div>
                        </setting>
                    </block>

                    <block title="Shipping defaults"
                           help="Default origin used when quoting from sales orders and Add shipping.">
                        <setting string="Default ship-from warehouse"
                                 help="Origin for Envia quotes. Uses the warehouse address (linked contact). Must include street, city, postal code, country, phone and email. Destination comes from the customer delivery address on the order.">
                            <field name="envia_default_origin_warehouse_id"
                                   options="{'no_create': True}"
                                   domain="[('company_id', '=', company_id)]"/>
                            <div class="text-muted small mt-2"
                                 invisible="not envia_default_origin_warehouse_id">
                                <span class="fw-bold">Linked contact:</span>
                                <field name="envia_default_origin_partner_display"
                                       class="d-inline"
                                       nolabel="1"
                                       readonly="1"/>
                            </div>
                            <div class="text-muted small mt-1"
                                 invisible="not envia_default_origin_address_preview">
                                <field name="envia_default_origin_address_preview"
                                       nolabel="1"
                                       readonly="1"
                                       widget="text"/>
                            </div>
                        </setting>
                        <setting>
                            <div class="o_envia_settings_toggle_stack">
                                <div class="o_envia_settings_toggle_row">
                                    <div class="o_envia_settings_toggle_copy">
                                        <div class="o_form_label">Use Envia as default shipping method</div>
                                        <div class="text-muted small">
                                            Pre-select Envia.com in Add shipping on sale orders.
                                        </div>
                                    </div>
                                    <field name="envia_default_carrier" nolabel="1"/>
                                </div>
                                <div class="o_envia_settings_toggle_row">
                                    <div class="o_envia_settings_toggle_copy">
                                        <div class="o_form_label">Enable branch pickup and delivery</div>
                                        <div class="text-muted small">
                                            Allow origin/destination branch pickup and drop-off.
                                            When disabled, only home delivery routes are available.
                                        </div>
                                    </div>
                                    <field name="envia_enable_branches" nolabel="1"/>
                                </div>
                            </div>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_envia_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="context">{'module': 'envia'}</field>
    </record>
</odoo>
